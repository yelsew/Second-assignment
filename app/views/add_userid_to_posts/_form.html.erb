<%= form_with(model: add_userid_to_post, local: true) do |form| %>
  <% if add_userid_to_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(add_userid_to_post.errors.count, "error") %> prohibited this add_userid_to_post from being saved:</h2>

      <ul>
      <% add_userid_to_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :add_userid_to_post_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
